name: storage_monitor_new
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.7.2 <4.0.0" # Dart SDKのバージョン範囲を明確に指定

dependencies:
  flutter:
    sdk: flutter

  # UI関連
  cupertino_icons: ^1.0.6
  dropdown_button2: ^2.3.9

  # ストレージ・デバイス情報
  path_provider: ^2.1.5 # 最新バージョンに更新
  device_info_plus: ^11.3.3 # 最新バージョンに更新

  # HTTP通信
  http: ^1.1.3 # 最新バージョンに更新

  # ローカルストレージ
  shared_preferences: ^2.2.0 # 最新バージョンに更新

  # バックグラウンド処理
  workmanager: ^0.5.2 # 最新バージョンに更新

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0 # 推奨される最新のLintルールセット

flutter:
  uses-material-design: true

# The following line ensures that the Material Icons font is
# included with your application, so that you can use the icons in
# the material Icons class.

# To add assets to your application, add an assets section, like this:
# assets:
#   - images/a_dot_burr.jpeg
#   - images/a_dot_ham.jpeg

# An image asset can refer to one or more resolution-specific "variants", see
# https://flutter.dev/to/resolution-aware-images

# For details regarding adding assets from package dependencies, see
# https://flutter.dev/to/asset-from-package

# To add custom fonts to your application, add a fonts section here,
# in this "flutter" section. Each entry in this list should have a
# "family" key with the font family name, and a "fonts" key with a
# list giving the asset and other descriptors for the font. For
# example:
# fonts:
#   - family: Schyler
#     fonts:
#       - asset: fonts/Schyler-Regular.ttf
#       - asset: fonts/Schyler-Italic.ttf
#         style: italic
#   - family: Trajan Pro
#     fonts:
#       - asset: fonts/TrajanPro.ttf
#       - asset: fonts/TrajanPro_Bold.ttf
#         weight: 700
#
# For details regarding fonts from package dependencies,
# see https://flutter.dev/to/font-from-package
